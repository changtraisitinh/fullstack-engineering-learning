apiVersion: apps/v1
kind: Deployment
metadata:
  name: lucky-money-service
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.luckyMoneyService.replicas }}
  selector:
    matchLabels: { app: lucky-money-service }
  template:
    metadata:
      labels: { app: lucky-money-service }
    spec:
      # Kubernetes injects <SVC>_PORT-style legacy Docker-links env vars for every
      # Service in the namespace by default; Confluent's cp-kafka entrypoint
      # misreads an injected KAFKA_PORT as a Kafka 'port' config property and
      # crashes. Disabling this (largely obsolete now that Service DNS exists)
      # avoids that class of bug everywhere, not just on kafka itself.
      enableServiceLinks: false
      containers:
        - name: lucky-money-service
          image: {{ include "ewallet-lab.image" (dict "root" $ "name" "lucky-money-service") }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.luckyMoneyService.port }}
          env:
            - name: DB_HOST
              value: postgres
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: ewallet_lucky_money
            - name: DB_USER
              valueFrom: { secretKeyRef: { name: postgres-credentials, key: POSTGRES_USER } }
            - name: DB_PASSWORD
              valueFrom: { secretKeyRef: { name: postgres-credentials, key: POSTGRES_PASSWORD } }
            - name: USER_SERVICE_URL
              value: "http://user-service:{{ .Values.userService.port }}"
            - name: WALLET_SERVICE_URL
              value: "http://wallet-service:{{ .Values.walletService.port }}"
            - name: LUCKY_MONEY_TTL_HOURS
              value: "{{ .Values.luckyMoneyService.ttlHours }}"
            - name: ALLOWED_ORIGIN_PATTERN
              value: {{ .Values.corsAllowedOriginPattern | quote }}
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: {{ .Values.luckyMoneyService.port }} }
            initialDelaySeconds: 15
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: {{ .Values.luckyMoneyService.port }} }
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: lucky-money-service
  namespace: {{ .Values.namespace }}
spec:
  selector: { app: lucky-money-service }
  ports:
    - port: {{ .Values.luckyMoneyService.port }}
      targetPort: {{ .Values.luckyMoneyService.port }}
