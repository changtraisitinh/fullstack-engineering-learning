apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-request-service
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.paymentRequestService.replicas }}
  selector:
    matchLabels: { app: payment-request-service }
  template:
    metadata:
      labels: { app: payment-request-service }
    spec:
      # Kubernetes injects <SVC>_PORT-style legacy Docker-links env vars for every
      # Service in the namespace by default; Confluent's cp-kafka entrypoint
      # misreads an injected KAFKA_PORT as a Kafka 'port' config property and
      # crashes. Disabling this (largely obsolete now that Service DNS exists)
      # avoids that class of bug everywhere, not just on kafka itself.
      enableServiceLinks: false
      containers:
        - name: payment-request-service
          image: {{ include "ewallet-lab.image" (dict "root" $ "name" "payment-request-service") }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.paymentRequestService.port }}
          env:
            - name: DB_HOST
              value: postgres
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: ewallet_payment_request
            - name: DB_USER
              valueFrom: { secretKeyRef: { name: postgres-credentials, key: POSTGRES_USER } }
            - name: DB_PASSWORD
              valueFrom: { secretKeyRef: { name: postgres-credentials, key: POSTGRES_PASSWORD } }
            - name: USER_SERVICE_URL
              value: "http://user-service:{{ .Values.userService.port }}"
            - name: TRANSFER_SERVICE_URL
              value: "http://transfer-service:{{ .Values.transferService.port }}"
            - name: PAYMENT_LINK_TTL_HOURS
              value: "{{ .Values.paymentRequestService.paymentLinkTtlHours }}"
            - name: ALLOWED_ORIGIN_PATTERN
              value: {{ .Values.corsAllowedOriginPattern | quote }}
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: {{ .Values.paymentRequestService.port }} }
            initialDelaySeconds: 15
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: {{ .Values.paymentRequestService.port }} }
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: payment-request-service
  namespace: {{ .Values.namespace }}
spec:
  selector: { app: payment-request-service }
  ports:
    - port: {{ .Values.paymentRequestService.port }}
      targetPort: {{ .Values.paymentRequestService.port }}
